#include <main.h>
/
 * main - entry block
 * @void: no argument
 * Return: 0
 /
int main (void)
{
        char Assel[] = "_putchar";

        int c;

        for (c = 0; c < 8; C++)
        {
        _putchar(Asse[c]);
        }
        _putchar('\n');
        return (0)
}
